runtime: python311
instance_class: F1

entrypoint: gunicorn -b :$PORT main:app

automatic_scaling:
  max_instances: 1  # Limits to 1 instance to control costs
  min_instances: 0  # Allows scaling to zero
  
# Configuration before 2021-02-01
#runtime: custom
#env: flex

#handlers:
#- url: /.*
#  script: auto

#runtime_config:
#  python_version: 3

#manual_scaling:
#  instances: 1
#resources:
#  cpu: 1
#  memory_gb: 0.5
#  disk_size_gb: 10

